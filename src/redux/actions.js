export const nextMonth = (data) => {
    return{
        type: 'nextMonth',
        // payload: data
    }
}

export const lastMonth = (data) => {
    return{
        type: 'lastMonth',
        // payload: data
    }
}