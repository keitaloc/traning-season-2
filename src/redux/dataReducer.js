const data = {
  id: [],
  list: [],
};

const dataReducer = (state = data, action) => {
  switch (state.type) {
    case "":
      return state;

    default:
      return state;
  }
};

export default dataReducer;
